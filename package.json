{
  "name": "winc-admin",
  "version": "1.6.0",
  "private": true,
  "scripts": {
    "dev": "bunx vite build --watch",
    "build": "bunx vite build",
    "zip": "bun run build && bash scripts/zip.sh"
  },
  "dependencies": {
    "uplot": "^1.6.31"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
